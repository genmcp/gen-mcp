apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: genmcp-demo
  labels:
    app: genmcp-demo
spec:
  to:
    kind: Service
    name: genmcp-demo
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None
