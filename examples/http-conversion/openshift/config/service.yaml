apiVersion: v1
kind: Service
metadata:
  name: genmcp-demo
  labels:
    app: genmcp-demo
spec:
  selector:
    app: genmcp-demo
  ports:
  - port: 80
    targetPort: 7007
    protocol: TCP
    name: http
  type: ClusterIP
